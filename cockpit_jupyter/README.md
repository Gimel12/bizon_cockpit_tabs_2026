## Cockpit - Jupyter Tab

* The project for a custom tab inside Cockpit for running jupyter notebooks inside the available containers inside the machine


## Current progress
* Automatically detect and filter all available docker images that could run jupyter notebooks
* Set the amount of GPUs to use inside the container
* Set the name for the docker container
* List all running containers
* Feature to open and stop containers
* Additional options in the run command